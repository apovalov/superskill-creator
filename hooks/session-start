#!/bin/bash
# Inject superskill-creator awareness into session
SKILL_DIR="$(cd "$(dirname "$0")/.." && pwd)/skills/superskill-creator"
if [ -f "$SKILL_DIR/SKILL.md" ]; then
  cat "$SKILL_DIR/SKILL.md"
fi
